# 中断集成本地修复日志

## 1. 修复范围

- 本地分支：`fix/interrupt-integration-local`
- 基线提交：`5180d0f`
- 远端策略：仅在本地修改和提交，不推送 GitHub
- 报告策略：暂不修改 `final_report_draft.md`
- 验证策略：每一步先做静态检查；功能仿真和上板验证留到 RTL 收敛后统一执行

## 2. 已确认的基线问题

1. `pipeline_cpu_top.v` 重复声明 `ex_div_result`，Vivado 无法编译 CPU。
2. `editable_pipeline_system_top.v` 使用了未显式声明的 `trap_taken`。
3. 外部中断锁存后没有确认清除路径。
4. 中断可在 DIV/Cache 事务未完成时进入，精确中断语义不完整。
5. MRET 在 EX 和后续 `shadow_restore` 阶段可能重复重定向 PC。
6. 一键检查脚本的源文件列表和最终综合顶层不正确。

## 3. 修改记录

### 步骤 1：清除静态编译阻断

修改文件：

- `src/pipeline_cycle/pipeline_cpu_top.v`
- `src/editable_pipeline_system_top.v`

修改内容：

1. 删除第二处重复的 `wire [31:0] ex_div_result;`，保留原有声明。
2. 在拨码顶层显式增加 `wire trap_taken;`，避免依赖 Verilog 隐式网络。

行为影响：

- 不改变数据通路、流水线控制或中断行为。
- 仅修复编译错误和隐式信号声明。

检查结果：

- Vivado 2018.3 `xvlog`：全部 Verilog/SystemVerilog 源文件分析通过。
- `uart_editable_pipeline_system_top`：静态 elaboration 通过。
- `editable_pipeline_system_top`：静态 elaboration 通过。
- 本步骤未运行功能仿真。

### 步骤 2：修正一键验收脚本的检查对象

修改文件：

- `vivado/run_all_tests.tcl`

修改内容：

1. 删除不存在的 `src/main/cpu_top.v`，改用实际的单周期 CPU 文件。
2. 补入 Cache、UART、数码管、手动加载器和最终 UART 顶层源文件。
3. 增加三个 UART testbench 到测试清单。
4. 将综合顶层改为 `uart_editable_pipeline_system_top`。
5. 将综合约束改为 `xdc/uart_pipeline_system.xdc`。

行为影响：

- 不改变 RTL。
- 修复“测试/综合对象不是最终系统”的验收缺口。

检查结果：

- 脚本引用的 87 个仓库路径全部存在。
- 不再引用 `src/main/cpu_top.v`。
- 最终综合顶层已指向 `uart_editable_pipeline_system_top`。
- 约束已指向 `xdc/uart_pipeline_system.xdc`。
- `git diff --check` 通过。
- 本步骤没有执行脚本中的功能仿真或布局布线。

### 步骤 3：增加外部中断确认与清除

修改文件：

- `src/extension_components/trap_csr_unit.v`
- `src/pipeline_cycle/pipeline_cpu_top.v`
- `src/editable_pipeline_system_top.v`
- `src/uart_editable_pipeline_system_top.sv`

修改内容：

1. trap 单元新增 `irq_external_ack`。
2. 只有 CPU 实际接受外部中断且 `mcause=MEIP` 时才产生确认脉冲。
3. 两个顶层收到确认后清除 `irq_external_latched`。
4. 外部请求继续采用锁存方式，因此等待期间不会因按键脉冲结束而丢失。

优先级：

```text
退出运行模式清除 > CPU确认清除 > 新按键请求置位
```

行为影响：

- 修复 MRET 恢复 MIE 后立刻重复进入同一个外部中断的问题。
- 不改变 timer IRQ 和 ECALL 的处理。

检查结果：

- 全部 RTL 源文件 `xvlog` 分析通过。
- UART 最终顶层静态 elaboration 通过。
- 拨码最终顶层静态 elaboration 通过。
- `tb_trap` 静态 elaboration 通过；新增输出在 testbench 中显式留空。
- 本步骤未运行功能仿真。
