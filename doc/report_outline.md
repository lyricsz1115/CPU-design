# 实验报告大纲

## 1. 实验目标与任务分析

说明题目 B 的基础要求和拓展要求，明确本组选择的方案：

- 基础部分：实现 RV32I 子集单周期 CPU。
- 拓展部分：实现五级流水线 CPU，并加入冒险处理。

## 2. RV32I 子集设计

说明本项目支持的指令：

```text
add, sub, and, or
addi
lw, sw
beq
jal
```

建议配一张表格，列出每条指令的类型、opcode、funct3、funct7 和作用。

## 3. 单周期 CPU 数据通路与控制器

说明以下模块：

- PC 与取指。
- 指令译码。
- 寄存器堆。
- 立即数生成。
- ALU。
- 数据存储器。
- 写回通路。
- 分支和跳转 PC 选择。

建议放一张单周期 CPU 数据通路图。

## 4. 五级流水线 CPU 设计

说明五级流水：

```text
IF -> ID -> EX -> MEM -> WB
```

说明四组流水线寄存器：

- IF/ID。
- ID/EX。
- EX/MEM。
- MEM/WB。

说明控制信号如何随流水线寄存器向后传递。

## 5. 冒险处理机制

重点写清楚三类机制：

- EX/MEM 到 EX 的数据前推。
- MEM/WB 到 EX 的数据前推。
- load-use 冒险暂停。
- branch 和 `jal` 的 flush。

建议配三组测试程序和仿真波形：

- `hazard.mem`：验证前推。
- `load_use.mem`：验证暂停。
- `branch.mem`：验证 flush。

## 6. Minisys 板级验证

说明：

- 顶层模块如何连接时钟、复位和 LED。
- 使用哪个程序上板演示。
- LED 或数码管显示的结果含义。
- Vivado 综合、实现和烧录流程。

## 7. 性能、面积、功耗分析

建议使用下面的 PPA 表格：

| 版本 | Fmax | LUT | FF | BRAM | 程序周期数 | CPI | 说明 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 单周期 CPU | 待测 | 待测 | 待测 | 待测 | 待测 | 约 1.0 | 组合路径较长 |
| 流水线 CPU | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 吞吐率更高，但控制更复杂 |

分析角度：

- 性能提升来自流水线提高吞吐率。
- 面积增加来自流水线寄存器、前推单元和冒险检测单元。
- 功耗可能随寄存器数量和控制逻辑增加而上升。
- 最终说明性能、功耗、面积之间的取舍。

## 8. 小组分工与贡献说明

建议按成员列出：

| 成员 | 负责模块 | 测试/文档贡献 | 主要成果 |
| --- | --- | --- | --- |
| A | 取指前端 | 立即数与 PC 测试 | IF/ID 与取指说明 |
| B | 控制与执行 | ALU 和前推测试 | 控制器与 EX 阶段 |
| C | 存储与上板 | load/store 和板级验证 | MEM/WB 与 Minisys 演示 |
| D | 集成与报告 | 顶层仿真和 PPA | 冒险检测、报告、PPT |

## 9. 总结与改进方向

可以写：

- 本项目完成了基础 CPU 和流水线拓展。
- 流水线提高了吞吐率，但增加了面积和控制复杂度。
- 后续可以继续扩展 Cache、乘除法、自定义指令或更完整的 RV32I 指令集。
